<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.oddcloud.www.oc_reload">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 获取sd卡写的权限，用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许读取手机状态 用于创建BmobInstallation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:uiOptions="splitActionBarWhenNarrow">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.DrawerActivity" />
        <activity android:name=".View.Activity.NewsAnd_IT_Detail_page_Activity" />
        <activity android:name=".View.Activity.TopicArticleList" />
        <activity android:name=".View.Activity.Login_with_Register" />
        <activity android:name=".View.Activity.About_Us" />
        <activity android:name=".View.Activity.Keeppage_Activity" />
        <activity android:name=".View.Activity.Personal_Setting" />
        <activity android:name=".View.Activity.Seeing_Page_History" />
        <activity android:name=".View.Activity.FeedBack" />
        <activity android:name=".View.Activity.SubjectArticleActivity" />
        <activity android:name=".View.Activity.HorizontalCoordinatorNtbActivity" />
        <activity android:name=".View.Activity.Site_Website_Page_list" />
        <activity android:name=".View.Activity.Sitelist_Website_context" />
        <activity android:name=".View.Activity.List_Collection_content" />
        <activity android:name=".View.Activity.User_detail_information"></activity>
    </application>

</manifest>